Pow is method(base as float, exponent as float) as float {
  return base ^ exponent;
}

Pow2 is method(value as float) as float {
  return value ^ 2;
}

Pow3 is method(value as float) as float {
  return value ^ 3;
}

