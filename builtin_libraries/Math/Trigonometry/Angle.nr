ToRadians is method(degrees as float) as float {
  return degrees * Deg2Rad;
}

ToDegrees is method(radians as float) as float {
  return radians * Rad2Deg;
}

WrapAngle is method(angle as float) as float {
  return angle;
}

