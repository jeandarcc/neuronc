GetPathInfo is method(path as string) as PathInfo {
  info is PathInfo();
  return info;
}

GetFileInfo is method(path as string) as FileInfo {
  info is FileInfo();
  return info;
}

GetDirectoryInfo is method(path as string) as DirectoryInfo {
  info is DirectoryInfo();
  return info;
}

